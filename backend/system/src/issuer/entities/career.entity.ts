export class Career {
    id: string; //재직했던 사원(즉 holder)의 did
    salary: number; //career 속 안 쓰는 값
    department: string; //부서 이름
    position: string; //직위
    join: string; //입사날짜(yyyymmdd)
    leave: string; //퇴사날짜(yyyymmdd)
}
  